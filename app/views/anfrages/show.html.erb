<div class="page-header">
  <h1>
    Anfrage für

      <small>
        <%= @event[0].name %>
      </small>

    <small>
      <small>
        von <%= @sender[0].email %>
      </small>
    </small>
  </h1>
</div>




<p class="lead">
  <%= @anfrage.titel %>
</p>

<p >
  <%= @anfrage.text %>
</p>

<% if @anfrage.absender_id != current_user.id %>
  <a href="new/antwort/<%= @anfrage.id %>">Antworten</a>
<% end %>