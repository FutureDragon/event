<%= form_for(@anfrage) do |f| %>
  <% if @anfrage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anfrage.errors.count, "error") %> prohibited this anfrage from being saved:</h2>

      <ul>
      <% @anfrage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titel %><br>
    <%= f.text_field :titel %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<p>
  Empfänger und Abender als Hidden einbauen
</p>